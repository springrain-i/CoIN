# CoIN 会话总结（2026-03-23）

## 1. 讨论目标
本次会话围绕 CoIN/LLaVA 中 LoRA 与 MoE-LoRA 的真实实现与研究方向展开，核心关注：

1. LoRA 在当前代码里到底插在哪些位置、如何生效。
2. 图文 token 如何组织，LoRA 是否同时作用于两种模态。
3. 持续学习中「LoRA 过度文本化」是否存在，以及如何做机制级验证。
4. MoE-LoRA 的专家是否随任务扩展、如何路由与训练。
5. 如何形成可投稿的论文思路与实验框架。



## 2. 代码层面的关键事实（基于仓库核对）

### 2.1 LoRA 注入位置
- LLaVA 分支默认通过 `find_all_linear_names(model)` 自动收集线性层作为 target modules。
- 该函数会排除 `mm_projector`、`vision_tower`、`vision_resampler`，并移除 `lm_head`。
- 因此不是只在 `q_proj/v_proj`，而是 LLM 内多处线性层（attention 与 MLP 相关线性层）都可能被 LoRA 注入。

### 2.2 图文 token 与 LoRA 作用范围
- 图像先经视觉编码器，再经 `mm_projector` 映射到语言空间。
- 文本 token 与视觉 token 在多模态准备函数中按 `IMAGE_TOKEN` 占位位置交错拼接（不是永远“文本在前，视觉在后”）。
- LoRA mask 里使用：`text=2, vision=1, pad=0`。
- 通过 `lora_mode` 可控制 LoRA 对哪些 token 生效：(**预实验添加的功能**)。
  - `all`：对 text+vision 生效
  - `text`：只对 text 生效
  - `vision`：只对 vision 生效
### 2.3 视觉编码器与 projector 训练状态
- CLIP vision tower 默认 `requires_grad_(False)`，通常冻结。
- `mm_projector` 可按训练参数设置为可训练或冻结。

### 2.4 MoE-LoRA（CoIN）机制
- 专家数由 `expert_num` 配置，当前实现是固定专家数，不会随任务自动扩容。
- 路由为 softmax 加权（soft routing），不是“任务固定选择单专家”。
- 持续学习阶段会加载上一任务 LoRA 权重并继续训练同一套 adapter（默认 adapter 名称）。

---

## 3. 研究问题聚焦（最终收敛）

要研究的核心不是“某一层 LoRA 是否重要”，也不应被 MoE 结构限定，而是：

1. 在标准 LoRA（不限定 MoE）下，模型是否相对 backbone 呈现文本偏置。
2. 该偏置是否在持续学习过程中随任务推进而加剧。
3. LoRA 的文本化程度是否显著高于 backbone 的天然文本偏置。
4. 这种加剧在行为、贡献、表示层面是否形成一致证据链。

> 叙事优先级建议：
> - 主线：LoRA vs backbone 的文本化对比（与是否 MoE 无关）
> - 扩展：在 MoE-LoRA 场景下进一步定位“路由/专家更新”是否放大该问题

---

## 4. 指标体系（从现象到机制）

> 讨论中形成共识：不能只看绝对值范数；应加入方向与功能指标。

### 4.1 行为层证据（现象）
- 比较 `all / text-only / vision-only / base`。
- 若 `text-only` 性能下降小、`vision-only` 下降大，支持“文本化偏置”现象。
- 与 backbone 做对照：
  - backbone（无 LoRA）对应同样的文本/视觉消融曲线。
  - 若 LoRA 组的“视觉去除敏感性下降更快”，可支持“LoRA 文本化更严重”。

### 4.2 贡献层证据（边际作用）
#### 动机
只看隐藏特征范数（例如 $||Δh||$）只能说明“变化幅度”，不能说明“是否真正改变决策”。
论文里需要回答的是：
1. 文本 LoRA 对最终正确类别是否更关键。
2. 视觉 LoRA 的边际作用是否随任务推进持续下降。

#### 数学定义
对同一样本、同一阶段，设正确类别为 $y$，记录四种前向模式下该类别 logit：
$z_y^{all}, z_y^{text}, z_y^{vision}, z_y^{base}$。

1. 纯边际贡献（相对 base）：
   - $MC_{text} = z_y^{text} - z_y^{base}$
   - $MC_{vis} = z_y^{vision} - z_y^{base}$

2. 互补边际贡献（在另一模态已启用时）：
   - $\widetilde{MC}_{text} = z_y^{all} - z_y^{vision}$
   - $\widetilde{MC}_{vis} = z_y^{all} - z_y^{text}$

3. 交互项（协同或冲突）：
   - $I = z_y^{all} - z_y^{text} - z_y^{vision} + z_y^{base}$

#### 如何计算（实践）
1. 每个任务阶段固定同一验证集。
2. 每个样本跑 all/text/vision/base 四次前向。
3. 样本级计算上述指标后求均值与置信区间（建议 bootstrap）。
4. 对每个阶段画曲线并拟合斜率。

#### 如何解读
1. 若 $MC_{vis}$ 或 $\widetilde{MC}_{vis}$ 随任务下降，而文本项稳定或上升，说明视觉边际贡献衰减。
2. 若 $I$ 很大，说明 text 与 vision 存在强协同或冲突，不能只用单模态结论解释整体性能。
3. 若 text-only 准确率接近 all，但 vision-only 明显差，同时 $MC_{text} \gg MC_{vis}$，是“功能层文本化”强证据。

#### 交互项 $I$ 的直觉解读（有点难懂部分）
交互项衡量的是“多模态一起用”是否等于“两个单模态贡献简单相加”。

1. 定义回顾：
  - $I = z_y^{all} - z_y^{text} - z_y^{vision} + z_y^{base}$

2. 三种情况：
  - $I \approx 0$：近似可加。text 与 vision 基本独立贡献，单模态结论可大致解释整体。
  - $I > 0$：正协同。两模态联合效果好于简单相加，存在互补增强。
  - $I < 0$：冲突/冗余。联合效果低于简单相加，可能出现模态竞争或干扰。

3. 数值例子：
  - 设 $z_y^{base}=1.0, z_y^{text}=2.0, z_y^{vision}=1.8, z_y^{all}=3.2$。
  - 则 $I=3.2-2.0-1.8+1.0=0.4>0$，说明 text 与 vision 在该样本上存在正协同。

4. 为什么对本课题重要：
  - 若只看 text-only/vision-only，不看 $I$，会忽略“模态间相互作用”这部分信息。
  - 在“LoRA 过度文本化”研究中，建议同时跟踪 $MC_{text}, MC_{vis}, I$ 随任务阶段的变化，避免把协同变化误判为单模态贡献变化。

#### 常见误区
1. 只看 accuracy 不看 logit 边际，容易丢失细粒度信号。
2. 不控制样本一致性会把数据分布变化误当成贡献变化。
3. 忽略交互项会把“协同效应”误判为单模态优势。

### 4.3 表示层证据（稳定性/漂移）（直接计算x经过LoRA的增量，根据增量方向分析，有一点怪怪的）
#### 动机
你研究的是“为什么会加剧”，而不仅是“是否加剧”。
表示层分析的作用是定位机制：LoRA 是否在持续学习中收缩到稳定文本方向，同时视觉方向不断被重写。

#### 数学定义
对第 $l$ 层、模态 $m \in \{text, vision\}$，定义 LoRA 增量表示：
$Δh_{l,m}^{(t)} = h_{l,m}^{(t)} - h_{l,m}^{(base,t)}$。

为避免单 token 噪声，定义该层该模态的“方向原型” $v_{l,m}^{(t)}$：
1. 对 token 增量先做单位化：$u = Δh/||Δh||$。
2. 再对所有样本和 token 取均值并归一化得到 $v$。

跨任务方向稳定性：
- $S_{l,m}^{(t)} = cos(v_{l,m}^{(t)}, v_{l,m}^{(t-1)})$

也可用子空间稳定性（更鲁棒）：
1. 对 $Δh_{l,m}^{(t)}$ 做 PCA 取前 $k$ 主方向子空间 $U_{l,m}^{(t)}$。
2. 比较 $U_{l,m}^{(t)}$ 与 $U_{l,m}^{(t-1)}$ 的主角度或投影相似度。

#### 如何计算（实践）
1. 固定层（例如每个 transformer block 输出）。
2. 分开统计 text token 与 vision token。
3. 每个任务阶段保存 $v_{l,m}^{(t)}$ 或 PCA 子空间。
4. 计算相邻阶段稳定性曲线 $S_{l,m}^{(t)}$。

#### 如何解读
1. 若 $S_{l,text}^{(t)}$ 长期高、$S_{l,vision}^{(t)}$ 低且波动大：
  - 文本方向更稳定（被反复强化）
  - 视觉方向更漂移（被反复覆盖）
2. 若该模式主要出现在中后层，往往对应你观察到的“文本捷径主导决策”。
3. 若稳定性变化与 $MC_{vis}$ 下降同步，机制链更完整（表示漂移 -> 功能衰减）。

#### 常见误区
1. 不区分 text/vision token 混合统计，会掩盖模态差异。
2. 只看一层结论不可靠，建议给出全层热图。
3. 只看范数不看方向，无法解释“为何文本化”。

### 4.4 MoE 专属证据（路由+训练信号）（MOE实际上就是加了多个LoRA，然后加权求和，可以总结为一个大LoRA,感觉目前没必要这么细）
> 本节为可选扩展，不是主线必需项。
> 若论文主目标是“LoRA 相对 backbone 的过度文本化”，可先完成 4.1-4.3；
> 若使用 MoE-LoRA 并希望解释其内部机制，再加入本节。

#### 动机
在 MoE-LoRA 中，“过度文本化”不仅可能发生在表示层，也可能发生在路由层：
1. Router 把文本 token 更频繁地送往高活跃专家。
2. 视觉 token 路由分布逐渐塌缩到少数专家。
3. 专家参数更新主要来自文本梯度。

#### 数学定义
设 router 输出为 $p_{l,e}(i)$（第 $l$ 层 token $i$ 分配给专家 $e$ 的软权重）。

1. 模态-专家利用率：
  - $U_{l,e}^{text} = \mathbb{E}_{i\in T_{text}}[p_{l,e}(i)]$
  - $U_{l,e}^{vis} = \mathbb{E}_{i\in T_{vis}}[p_{l,e}(i)]$

2. 偏置分数：
  - $B_{l,e} = (U_{l,e}^{text} - U_{l,e}^{vis}) / (U_{l,e}^{text} + U_{l,e}^{vis} + \epsilon)$

3. 模态平均路由分布：
  - $\bar p_{l,e}^{m} = \mathbb{E}_{i\in T_m}[p_{l,e}(i)]$, $m\in\{text,vis\}$

4. 熵与有效专家数：
  - $H_l^m = -\sum_e \bar p_{l,e}^m \log \bar p_{l,e}^m$
  - $N_{eff,l}^m = \exp(H_l^m)$

5. 梯度偏置：
  - $R_{l,e}^{grad} = ||\nabla_{\theta_{l,e}} L_{text}|| / (||\nabla_{\theta_{l,e}} L_{vis}|| + \epsilon)$

#### 如何计算（实践）
1. 在 MoE-LoRA 前向中记录 router softmax 权重（按层、按专家、按 token）。
2. 用现有 token mask 区分 text 与 vision token，分别聚合 $U$、$\bar p$、$N_{eff}$。
3. 训练时对专家参数梯度做模态拆分统计（或通过 text-only / vision-only 小批次近似）。
4. 随任务阶段绘制层-专家热图与趋势曲线。

#### 如何解读
1. 若多数层专家满足 $B_{l,e}$ 上升，说明 router 逐步偏文本。
2. 若 $N_{eff,l}^{vis}$ 持续下降，说明视觉路由塌缩（视觉信息被少数专家承载）。
3. 若 $R_{l,e}^{grad}$ 持续上升，说明专家参数更新越来越由文本主导。
4. 若上述现象与 $MC_{vis}$ 下降同步，可形成“路由偏置 -> 更新偏置 -> 功能衰减”的机制闭环。

#### 常见误区
1. MoE 是 soft routing，不能用“被选中次数”替代权重统计。
2. 只看 router 权重不看梯度与功能，证据链不完整。
3. 不做任务时间轴只做单阶段比较，无法支持“加剧”结论。

---

## 5. 关键方法论澄清（本次讨论中的重要纠偏）

### 5.1 不能只做“单层局部值”
你指出了正确问题：每层输入都已混入前层 LoRA 影响，因此单层局部差值不够宏观。

因此建议并行使用：
1. 局部边际效应（该层净注入）
2. 累计总效应（到该层为止偏移）
3. 反事实全局边际（关闭某层/某专家后对最终输出的变化）

### 5.2 功能指标不只在最后一层
- 最终准确率确实在输出层体现。
- 但中间层可通过线性映射后变化、注意力 logits 变化来定义“功能影响”。

---

## 6. 可执行实验路线（建议）

### 阶段 A：诊断（先不改模型）
1. 在每个任务阶段固定同一验证集。
2. 采集两组模型：
  - Backbone 组（无 LoRA）
  - LoRA 组（你的训练设置）
3. 对每组都计算 all/text/vision/base 指标。
4. 先完成 4.1-4.3 的主线证据。
5. 如使用 MoE-LoRA，再补 4.4 的扩展证据。
6. 绘制随任务阶段变化曲线，验证“LoRA 是否比 backbone 更文本化且更快加剧”。

### 阶段 B：机制验证
1. 做反事实干预：关闭层/专家，观察最终 logit 与精度变化。
2. 验证文本化是否主要由某些层或某些专家驱动。

### 阶段 C：方法改进（可作为论文第二部分）
可选方向：
1. 任务条件路由（task-conditioned router）
2. 专家扩容策略（按任务增量新增专家）
3. 模态平衡正则（抑制文本独占）
4. 视觉专属分支或蒸馏约束

---

## 7. 目前形成的结论（会话级）

1. 你最初对 LoRA 工作流的理解大方向正确，但“仅 Q/V 生效”在本代码里不成立。
2. 当前实现中，LoRA 通过 token mask 支持 text/vision/all 三种生效模式。
3. 持续学习中的“过度文本化”可通过行为、贡献、表示、路由、梯度五条证据链闭环验证。
4. MoE-LoRA 当前不是“任务绑定单专家”，而是 token 级 soft routing；专家数固定配置，不会自动随任务扩展。

---

## 8. 后续建议（实操优先级）

1. 先完成“LoRA vs backbone”对照诊断（4.1-4.3），拿到主结论硬证据。
2. 再做小规模层级反事实（阶段 B）确认加剧来源。
3. 若使用 MoE，再补 4.4 作为机制加分项。
4. 最后做轻量改进（如模态平衡正则）验证可缓解文本化。

这样可以最快形成一篇“机制发现 + 初步干预”的论文骨架。
